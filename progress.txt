## Codebase Patterns
- Android Gradle wrapper: generate with `gradle wrapper --gradle-version=8.4`; wrapper JAR and scripts not committed (generated locally)
- Android compilation: requires JDK 17 (not newer versions), use `JAVA_HOME` env var to point to correct JDK
- API enrichment modules follow the same pattern: async function returns a dataclass, uses `urllib.request` with `loop.run_in_executor()`, `_parse_*` helper for response processing
- Android WebSocket client: OkHttp with pingInterval(30s), ConnectionState sealed class, token handshake on open
- Android buildConfig stored as buildConfigField in app/build.gradle.kts (WS_HOST, WS_PORT, WS_TOKEN)
- Android landscape mode forced via `android:screenOrientation="landscape"` in manifest
- LAN cleartext traffic enabled via `android:usesCleartextTraffic="true"` in manifest
- Android Gradle stack: AGP 8.2.2, Kotlin 1.9.22, Compose BOM 2024.02.00, OkHttp 4.12.0
- Android project structure: module under `android/` with standard Gradle layout
- Use `urllib.request` with `loop.run_in_executor()` for async HTTP calls (avoids adding httpx/aiohttp deps)
- Server entry point: `src/server.py` with `asyncio.run(main())`
- Authentication: token-based handshake in `src/auth.py`, verified via `verify_token()`
- Config from environment variables in `src/config.py`
- Use `asyncio.wait_for()` with TimeoutError for handshake timeout
- Quality checks: `ruff check src/ tests/ && mypy src/ tests/ && pytest tests/ -v`
- CLI tool wrappers use `asyncio.create_subprocess_exec()` in `src/playerctl.py`
- Import from `collections.abc` instead of `typing` for generic types (AsyncIterator, Awaitable, etc.)
- Tests import from `src` package — run with `PYTHONPATH=.` or `pip install -e .`
- Server broadcast pattern: manage client set with `difference_update()` to avoid ruff F823 (local var reference before assignment) – iterate over `list(clients)` snapshot when removing during iteration
- Command dispatch: use `dict[str, Awaitable[None]]` mapping with eagerly created coroutines, accessed via `.get()` to avoid IndexError

## [06/05/2026 17:30] - US-001
- What was implemented: Backend scaffolding with WebSocket server and token-based authentication
- Files changed: src/__init__.py, src/server.py, src/auth.py, src/config.py, requirements.txt, pyproject.toml, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: project uses `src/` package structure with config from env vars
  - Gotchas encountered: `asyncio.TimeoutError` should use builtin `TimeoutError` to pass ruff lint (UP041)
  - Useful context: `websockets` v16 library used with `websockets.serve()` pattern
---

## [06/05/2026 18:00] - US-002
- What was implemented: playerctl integration module for monitoring Brave PWA playback
- Files changed: src/playerctl.py, tests/test_playerctl.py, requirements.txt, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: Use `asyncio.create_subprocess_exec()` for CLI tool wrappers (playerctl)
  - Patterns discovered: Use `collections.abc.AsyncIterator` instead of `typing.AsyncIterator` (ruff UP035)
  - Gotchas encountered: Pytest needs PYTHONPATH or `pip install -e .` to import from `src` package
  - Useful context: playerctl follow outputs lines in `<instance>: <status/metadata>` format
  - Useful context: `playerctl metadata --format "{{title}} — {{artist}}"` outputs formatted metadata
---

## [06/05/2026 18:30] - US-003
- What was implemented: iTunes Search API enrichment module for HD album artwork and track duration
- Files changed: src/itunes.py, src/playerctl.py, tests/test_itunes.py, requirements.txt, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: Use `urllib.request` with `loop.run_in_executor()` for async HTTP calls (avoids httpx/aiohttp deps)
  - Gotchas encountered: Tests need `PYTHONPATH=.` or editable install to import from `src` package
  - Gotchas encountered: mypy strict mode catches `Returning Any` - explicitly type intermediate vars to fix
  - Gotchas encountered: `pytest-asyncio` must be installed for `@pytest.mark.asyncio` tests to work
  - Useful context: iTunes Search API returns `artworkUrl100` - replace `100x100` with `600x600` for HD
   - Useful context: iTunes Search API field `trackTimeMillis` provides accurate track duration
---

## [06/05/2026 19:00] - US-004
- What was implemented: LRCLIB API enrichment module for synchronized lyrics
- Files changed: src/lrclib.py, tests/test_lrclib.py, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: Follow the same module pattern as itunes.py for API enrichment (async function returning dataclass)
  - Gotchas encountered: LRCLIB API returns `syncedLyrics` and `plainLyrics` as separate fields — use `syncedLyrics` if available, fall back to `plainLyrics`
  - Gotchas encountered: LRCLIB API requires a User-Agent header or it returns 403
   - Useful context: LRC timestamp format `[mm:ss.xx]` parsed with regex; millisecond precision varies (1-3 digits)
---

## [06/05/2026 19:30] - US-005
- What was implemented: Unified JSON broadcast and command handling
- Files changed: src/playerctl.py, src/server.py, tests/test_server.py, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: Server uses `asyncio.create_task()` for background monitor loop; module-level sets for client tracking
  - Patterns discovered: Import `Awaitable` from `collections.abc` for typing command dispatch dicts
  - Gotchas encountered: `clients -= disconnected` triggers ruff F823 (local var reference before assignment) — use `clients.difference_update()` and iterate over `list(clients)` snapshot
  - Gotchas encountered: Eagerly created coroutines in command_map produce "never awaited" warnings in tests — acceptable since mocked send_* functions are patched, real code awaits only the selected one
  - Useful context: Playerctl position outputs seconds as float string — multiply by 1000 for ms
   - Useful context: Album metadata fetched separately via `playerctl metadata --format "{{album}}"` 
---

## [06/05/2026 20:00] - US-006
- What was implemented: Android project scaffolding with WebSocket client and connection state management
- Files changed: .gitignore, prd.json, progress.txt, android/AGENTS.md, android/build.gradle.kts, android/settings.gradle.kts, android/gradle.properties, android/gradle/wrapper/gradle-wrapper.properties, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, android/app/src/main/res/values/strings.xml, android/app/src/main/res/values/themes.xml, android/app/src/main/java/com/mediacontrol/app/MainActivity.kt, android/app/src/main/java/com/mediacontrol/app/websocket/WebSocketClient.kt
- **Learnings for future iterations:**
  - Patterns discovered: Android project uses standard Gradle layout under `android/` with AGP 8.2.2 and Kotlin 1.9.22
  - Patterns discovered: Server config (host, port, token) accessed via BuildConfig fields defined in `defaultConfig.buildConfigField`
  - Patterns discovered: Connection states modeled as sealed class for exhaustive `when` matching in Compose
  - Gotchas encountered: OkHttp WebSocket authentication is implicit — first received message after handshake confirms auth, not an explicit auth response
  - Gotchas encountered: Android 10 needs `android:usesCleartextTraffic="true"` for LAN ws:// connections
  - Useful context: OkHttp `pingInterval(30, TimeUnit.SECONDS)` keeps WebSocket alive on Android
   - Useful context: Gradle wrapper needs to be generated (`gradle wrapper`) before `./gradlew` works — wrapper JAR and scripts not committed (generated locally)
---

## [06/05/2026 21:00] - US-007
- What was implemented: Data models (PlaybackState, UiState) and PlaybackViewModel with WebSocket connect/parse/expose StateFlow
- Files changed: android/app/src/main/java/com/mediacontrol/app/model/PlaybackState.kt, android/app/src/main/java/com/mediacontrol/app/model/UiState.kt, android/app/src/main/java/com/mediacontrol/app/viewmodel/PlaybackViewModel.kt, android/app/src/main/java/com/mediacontrol/app/MainActivity.kt, android/AGENTS.md, prd.json, progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: `kotlinx.serialization.json.buildJsonObject` with `put()` used for building command JSON in ViewModel
  - Patterns discovered: ViewModel stores `token` as field for command authentication
  - Gotchas encountered: `sendCommand` must include `token` field in command payload for server validation
  - Gotchas encountered: `gradle wrapper` requires JDK 17; Java 26 causes "26.0.1" error from Gradle 8.x
  - Gotchas encountered: AGP 8.2.2 auto-installs missing build-tools (34.0.0) during first build
  - Useful context: `compileDebugKotlin` passes without emulator — just validates Kotlin compilation
---
